<div class="wrapperuser padded-top text-center">
  <ul>
    <% avatar_url = current_user.facebook_picture_url || "clown.png" %>

      <% if avatar_url == current_user.facebook_picture_url %>
        <%= image_tag avatar_url, class: "avatar-large dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
      <% elsif current_user.photo == nil %>
        <%= image_tag avatar_url, class: "avatar-large dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
      <% else %>
        <%= cl_image_tag current_user.photo.path, class: "avatar-large dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
      <% end %>

    <li><%= @user.user_name %></li>
    <li><%= @user.last_name %></li>
    <li><%= @user.first_name %></li>
    <div>
      <% if @user == current_user %>
        <%= link_to "Edit my profile", edit_user_registration_path %>
      <% end %>
    </div>
  </ul>
  <div class="tabs">
    <a class="tab active " data-target="#my-bets">
      <h3>Mes paris </h3>
    </a>
    <a class="tab" data-target="#my-stats">
      <h3>Mes stats</h3>
    </a>
    <a class="tab" data-target="#my-challenges">
      <h3>Mes challenges</h3>
    </a>
  </div>
</div>

<div class="container text-center borders">
  <div id="my-bets" class="tab-content">
    <div class="row">
      <div class="col-xs-12 col-sm-6 col-md-3 borders">
        <h4>Publics</h4>
          <div class="dashboard-card">
            "<%= Bet.all.sample.title %>"
            <li><%= rand(1..100) %> Pts misé(s)</li>
          </div>
          <div class="dashboard-card">
            "<%= Bet.all.sample.title %>"
            <li><%= rand(1..100) %> Pts misé(s)</li>
          </div>
          <div class="dashboard-card">
            "<%= Bet.all.sample.title %>"
            <li><%= rand(1..100) %> Pts misé(s)</li>
          </div>
      </div>
      <div class="col-xs-12 col-sm-6 col-md-3 borders">
        <h4>Privés</h4>
      </div>
      <div class="col-xs-12 col-sm-6 col-md-3 borders">
        <h4>En cours</h4>
          <div class="dashboard-card">
            "<%= Bet.all.sample.title %>"
            <li><%= rand(1..100) %> Pts misé(s)</li>
          </div>
          <div class="dashboard-card">
            "<%= Bet.all.sample.title %>"
            <li><%= rand(1..100) %> Pts misé(s)</li>
          </div>
          <div class="dashboard-card">
            "<%= Bet.all.sample.title %>"
            <li><%= rand(1..100) %> Pts misé(s)</li>
          </div>
      </div>
      <div class="col-xs-12 col-sm-6 col-md-3 borders">
        <h4>Terminés</h4>
        <div class="dashboard-card-finish">
            "<%= Bet.all.sample.title %>"
            <p> <%= link_to 'Valider le pari', bet_path, class: "btn btn-dashboard"%> </p>
            <div class="texthelp">
               <p>Le pari est terminé, vous devez le valider en donnant le resultat.</p>
            </div>
        </div>
      </div>
    </div>
  </div>
  <div id="my-stats" class="tab-content hidden">
    <div class="row">
      <div class="col-xs-12 col-sm-6 col-md-3 borders">
        <h4>Paris gagnés</h4>
          <div class="dashboard-card">
            "<%= Bet.all.sample.title %>"
            <li><%= rand(1..100) %> Pts misé(s)</li>
          </div>
          <div class="dashboard-card">
            "<%= Bet.all.sample.title %>"
            <li><%= rand(1..100) %> Pts misé(s)</li>
          </div>
      </div>
      <div class="col-xs-12 col-sm-6 col-md-3 borders">
        <h4>Paris perdus</h4>
          <div class="dashboard-card">
            "<%= Bet.all.sample.title %>"
            <li><%= rand(1..100) %> Pts misé(s)</li>
          </div>
          <div class="dashboard-card">
            "<%= Bet.all.sample.title %>"
            <li><%= rand(1..100) %> Pts misé(s)</li>
          </div>
      </div>
      <div class="col-xs-12 col-sm-6 col-md-3 borders">
        <h4>Taux de réussite</h4>
          <div class="dashboard-card-taux">
            <h1> 10 %</h1>
          </div>
      </div>
      <div class="col-xs-12 col-sm-6 col-md-3 borders">
        <h4>Classement</h4>
          <div class="dashboard-card-taux">
            <h1> 10/1200 </h1>
          </div>
      </div>
    </div>
  </div>
  <div id="my-challenges" class="tab-content hidden">
    <div class="row">
      <div class="col-xs-12 col-sm-6 col-md-6 borders">
        <h4>En cours</h4>
          <div class="dashboard-card">
            <%= Bet.all.sample.challenge_title %>
          </div>
      </div>
      <div class="col-xs-12 col-sm-6 col-md-6 borders">
        <h4>Terminés</h4>
      </div>
    </div>
  </div>
</div>










