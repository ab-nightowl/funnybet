<div class="wrapper body-bet">
  <div class="container padded">
    <div class="col-xs-3 text-center">
      <div class="beters-container">
       <h4>Parieurs</h4>
        <div class="margin_avatar">
          <%= image_tag 'logo.png', class: "avatar-medium center-block" %>
          <br>
          <%= image_tag 'logo.png', class: "avatar-medium center-block" %>
          <br>
          <%= image_tag 'logo.png', class: "avatar-medium center-block" %>
          <br>

        </div>

      </div>
    </div>
    <div class="col-xs-9">
      <div class="row text-center description_background padded-text">
        <%= simple_form_for @bet do |f| %>
          <%= f.input :title, label: "Donne un titre à ton pari" %>
          <%= f.input :description, label: "Pour plus de détails (si tu veux)" %>
        <% end %>
      </div>
      <div class="row text-center challenge_background padded-text">
        <%= simple_form_for @bet do |f| %>
          <%= f.input :challenge_title, label: "Donne une indication sur le gage pour le(s) perdant(s) ;)" %>
          <%= f.input :challenge, label: "C'est quoi ton gage ?" %>
        <% end %>
      </div>
      <div class="row text-center">
        <div class="deadline padded-text">
        <strong><p><%= @bet.finish_at %></p></strong>
        <%= simple_form_for @bet do |f| %>
          <%= f.input :finish_at, label: "Donne une date de cloture du pari" %>
        <% end %>
        </div>
      </div>
      <div class="row text-center text-align">
        <div class="flex_odd padded-text">
          <%#= simple_form_for @choice do |f| %>
            <%#= f.input :title %>
          <%# end %>

          <!-- <div>
            <p><%= link_to "OUI", "#", class: "btn btn-default bet_button" %></p>
            <p>cote1</p>
          </div>
          <div>
            <p><%= link_to "NUL", "#", class: "btn btn-default bet_button" %></p>
            <p>cote2</p>
          </div>
          <div>
            <p><%= link_to "NON", "#", class: "btn btn-default bet_button" %></p>
            <p>cote3</p>
          </div> -->
        </div>
      </div>
      <div class="row bet_amount_background padded-text">
        <div>
          <p>Lache ton fric :</p>
        </div>
      </div>

    </div>
  </div>
</div>
